import { strictEqual } from 'assert'

describe('createTable', () => {
  it('basic test', () => {
      strictEqual(0, 0)
  })
  it('basic test FAIL', () => {
      strictEqual(0, 2)
  })
})
console.log('Run')
